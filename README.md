This file contains details and instructions to build and run photo-service REST service.

1. To build
   mvn clean package

2. To start service
   java -jar target/photo-service-0.0.1-SNAPSHOT.jar server photo-service.yml
